PERPLEXITY_API_KEY="pplx-REPLACE_ME"
# Optional: override Perplexity model (defaults to "sonar")
PERPLEXITY_MODEL="sonar"

# Optional OpenAI keys (not required by default — the app uses local embeddings)
# OPENAI_API_KEY="sk-REPLACE_ME"
# CHROMA_OPENAI_API_KEY="sk-REPLACE_ME"
